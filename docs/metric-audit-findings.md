# Metric Audit Findings and Phase 2 Proposal

This synthesis is generated from the 89 reviewed records in `audit/metrics.yaml`. It does not revise individual findings or approve runtime changes.

## Review-priority summary

Priority is a synthesis triage: High covers possible defects, Medium covers definition variants and validation gaps, and Low covers documentation, tests, overlaps, and consistent results.

| Priority | Findings |
| --- | ---: |
| High | 16 |
| Medium | 45 |
| Low | 58 |

## Evidence-backed possible defects

| Metric record | Evidence | Impact | Reviewed future action |
| --- | --- | --- | --- |
| [ `MASE` ](metric-audit.md#metric-mase) | The public m parameter is ignored and unvalidated. | Seasonal requests silently use lag 1. | Validate and implement m separately. |
| [ `CI` ](metric-audit.md#metric-ci) | Delegated WIA divides by zero for all-zero agreement. | CI raises instead of returning a score. | Address WIA separately. |
| [ `FB` ](metric-audit.md#metric-fb) | Runtime uses a prediction-minus-observation pointwise mean, while cited FB is an observation-minus-prediction ratio of means. | Both aggregation and sign differ from canonical FB. | Confirm intended convention before changing runtime behavior. |
| [ `MAGE` ](metric-audit.md#metric-mage) | Runtime divides each absolute residual by observation, while authoritative MAGE is unnormalized. | The method implements a fractional normalized metric rather than dimensional MAGE. | Confirm the intended registry name and formula before changing behavior. |
| [ `TS` ](metric-audit.md#metric-ts) | Exact agreement yields 0/0 and NaN despite a documented ideal of zero. | The best possible input does not return the stated ideal. | Decide separately whether exact zero residuals should map to zero. |
| [ `RAE` ](metric-audit.md#metric-rae) | Runtime computes relative L2 error, not conventional relative absolute error against the observed-mean baseline. | Ranking, denominator failure, and numeric values differ from RAE. | Confirm intended metric before any separately approved rename or formula change. |
| [ `VAF` ](metric-audit.md#metric-vaf) | Runtime equals 100 times SBF rather than 100 * (1 - Var(error)/Var(observation)). | It can exceed 100 and measures regression slope, not accounted variance. | Confirm intended convention before any separately approved formula change. |
| [ `DE` ](metric-audit.md#metric-de) | The primary paper defines unbounded DE as a distance with ideal zero; runtime silently returns the implementation-specific transformation 1-DE under the same DE identity, and no primary support was established for that naming. | The score's direction, ideal, numeric value, and range differ from canonical DE although its components are recognizable. | Confirm intent before either restoring canonical DE or explicitly renaming and documenting the transformed score in a separately approved runtime change. |
| [ `WIAr` ](metric-audit.md#metric-wiar) | The source's A>B branch is B/A-1, but runtime returns 1-B/A. | Poor fits that should be negative are reported positive, collapsing implemented range to nonnegative values. | Correct the branch only in a separately approved behavior change with migration notes. |
| [ `OVER` ](metric-audit.md#metric-over) | Espinar OVER uses max(abs(F_prediction-F_observation)-Vc,0); runtime omits Vc and uses max(F_prediction-F_observation,0). | Runtime is directional and reports area for gaps below the canonical threshold, so it is not canonical OVER. | Confirm intent before correcting or renaming in a separately approved runtime change. |
| [ `NMBF` ](metric-audit.md#metric-nmbf) | Runtime NMBF returns a raw ratio with ideal one; Yu et al. define a piecewise symmetric factor bias with ideal zero. | Runtime values, ideal, symmetry, and domain handling differ from the named primary-source metric. | Confirm intent before restoring the canonical piecewise formula or renaming in a separately approved runtime change. |
| [ `AD` ](metric-audit.md#metric-ad) | Runtime is directional and lacks pooled-CDF integration and sample-size normalization used by the named k-sample Anderson-Darling statistic. | Magnitude and even symmetry differ from standard AD two-sample interpretations. | Confirm whether a custom distance was intended; otherwise consider a separately approved canonical implementation or rename. |
| [ `TAcc` ](metric-audit.md#metric-tacc) | The 1e-10 stabilizer makes a varying forecast against a flat observation trend return roughly -1e10, and one-point input reaches a low-level polyfit failure. | The score is numerically unstable near a zero observed slope and short inputs fail unclearly. | Define the intended zero-trend and minimum-length policy before a separately approved runtime fix. |
| [ `iqRMSE` ](metric-audit.md#metric-iqrmse) | Runtime returns infinity for 0/0 when both RMSE and observed IQR are zero. | A perfect constant forecast is reported as infinitely bad rather than undefined or perfect. | Specify zero-IQR semantics before any separately approved runtime change. |
| [ `MSLE` ](metric-audit.md#metric-msle) | Negative values are not rejected; domain-invalid contributions can be omitted by nanmean and make a partially invalid dataset appear perfect. | MSLE can return a finite misleading value after runtime warnings. | Specify a nonnegative input contract and consider explicit validation only in a separately approved runtime change. |
| [ `NAE` ](metric-audit.md#metric-nae) | The denominator is the signed half-sum rather than a magnitude, so a named absolute error can be negative and can diverge at opposite-signed pairs. | The documented nonnegative error interpretation and comparisons across signed data are unreliable. | Document the signed denominator immediately; evaluate an absolute-denominator alternative only as a separately approved compatibility change. |

## Definition variants

| Metric record | Evidence | Impact | Reviewed future action |
| --- | --- | --- | --- |
| [ `KendallTau` ](metric-audit.md#metric-kendalltau) | Runtime uses SciPy's default tau-b, while the method docstring displays the untied tau-a-style denominator 'total pairs'. | Results with ties do not follow the displayed formula even though tau-b is a standard variant. | Update documentation to name tau-b and show or link its tie-adjusted denominator. |
| [ `EV` ](metric-audit.md#metric-ev) | The package's shared safe division maps all constant-target denominators to NaN. The raw formula is already NaN for zero residual variance (perfect or constant-offset predictions), but is negative infinity for positive residual variance; some APIs instead force finite scores. | The implementation collapses raw negative-infinity varying-prediction cases into NaN, while preserving NaN for raw 0/0 cases. | Document the current NaN policy before considering any future behavior change. |
| [ `NMSE` ](metric-audit.md#metric-nmse) | The generic name NMSE does not identify the implemented air-quality product-of-means normalization among several established normalizations. | Users can compare incompatible quantities under the same abbreviation. | Document the exact denominator and application domain. |
| [ `CRM` ](metric-audit.md#metric-crm) | The numerator sign is opposite Loague and Green. | Bias direction is reversed. | Document the sign or change it only in a versioned fix. |
| [ `MASE` ](metric-audit.md#metric-mase) | Scaling uses evaluation observations rather than a training series. | Results can differ from forecast MASE. | Document the available-data variant. |
| [ `MNB` ](metric-audit.md#metric-mnb) | Runtime returns a fraction rather than percent units. | Values differ by factor 100 from percent reports. | Document units. |
| [ `MFB` ](metric-audit.md#metric-mfb) | A (0,0) pair contributes zero and remains in N, unlike legacy FB which omits it. | MFB and FB differ when zero pairs occur despite sharing the ordinary-data formula. | Document zero-pair weighting and naming distinction. |
| [ `MFE` ](metric-audit.md#metric-mfe) | A (0,0) pair counts as zero, whereas FAE omits it. | MFE and FAE differ only for zero pairs or negative-domain validation. | Document the distinction. |
| [ `FAC2` ](metric-audit.md#metric-fac2) | Runtime multiplies the conventional fraction by 100. | Values and ideal differ by factor 100. | Document percentage units. |
| [ `MBD` ](metric-audit.md#metric-mbd) | The implementation is percentage relative MBD despite the unqualified dimensional name. | Output units and scale differ from MBD by division by the observation mean and factor 100. | Rename or document the normalization and units. |
| [ `RMSD` ](metric-audit.md#metric-rmsd) | The implementation returns percentage relative RMSD despite the unqualified dimensional name. | Scale and units differ from canonical RMSD. | Rename or document the normalization. |
| [ `MAD` ](metric-audit.md#metric-mad) | Runtime adds signed-mean normalization and factor 100 to dimensional mean absolute difference. | Output units, scale, and range differ from the registered name. | Document percentage-relative normalization or rename the metric. |
| [ `SD` ](metric-audit.md#metric-sd) | Runtime computes percent-normalized population residual spread without a degrees-of-freedom correction. | It differs from fitted-model residual standard deviation and is insensitive to constant bias. | Document the population convention, normalization, and distinction from RSE. |
| [ `U95` ](metric-audit.md#metric-u95) | Runtime uses percentage-normalized components rather than dimensional SD and RMSE. | Output is a relative percent composite rather than data-unit uncertainty. | Document component units and formula. |
| [ `KSI` ](metric-audit.md#metric-ksi) | Runtime uses a left-rectangle sum on the pooled discontinuity grid and fixed 1.63 critical factor; source presentations describe the integral and commonly trapezoidal numerical evaluation. | Discrete numerical values and normalization depend on grid and convention. | Document the exact grid, rectangle rule, confidence factor, and one-sample N convention. |
| [ `SUSE` ](metric-audit.md#metric-suse) | Runtime fixes natural logarithms and NumPy equal-width automatic ranges; published entropy comparisons permit bin-resolution and normalization choices. | Values are resolution- and convention-dependent. | Document natural-log units and exact common/separate edge construction. |
| [ `IQR` ](metric-audit.md#metric-iqr) | Runtime inherits NumPy's installed default quantile interpolation, one of multiple accepted sample-quantile conventions. | Small-sample IQR can vary across conventions or dependency-era defaults. | Document the linear percentile convention. |
| [ `nESkew` ](metric-audit.md#metric-neskew) | Runtime normalizes by signed max(prediction), so all-negative predictions reverse skewness sign although positive rescaling normally leaves skewness invariant. | Reported asymmetry direction depends on prediction sign convention. | Confirm and document the normalization definition before any behavior change. |
| [ `MBF` ](metric-audit.md#metric-mbf) | Runtime computes prediction mean divided by observation mean; the cited MBF source uses the reciprocal orientation. | Over- and underprediction interpretation reverses across otherwise recognizable MBF definitions. | Document prediction/observation orientation and that the positivity restriction applies to means, not every individual value. |
| [ `CPI` ](metric-audit.md#metric-cpi) | Published CPI uses normalized KSI/OVER and relative RMSD; runtime explicitly requests raw KSI/OVER and raw RMSE. | Runtime CPI has data units and is not comparable to the published percentage index. | Document the raw-unit variant and dependency settings before considering any separately approved change. |
| [ `RED` ](metric-audit.md#metric-red) | Runtime computes RMS pointwise relative error; the cited EPRI RED instead takes the quadrature of normalized mean difference, normalized spread difference, and correlation discrepancy. | Runtime measures paired pointwise relative residuals, while cited RED summarizes differences in mean, variability, and association; their values and interpretation are not interchangeable. | Document the exact RMS-relative formula or rename before any runtime change. |
| [ `U2` ](metric-audit.md#metric-u2) | Runtime normalizes by observation RMS rather than by the error of a lagged naive forecast. | Zero and one do not carry canonical U2's naive-benchmark interpretation. | Rename or document the implemented normalized RMSE; change behavior only with compatibility approval. |
| [ `lambda` ](metric-audit.md#metric-lambda) | Runtime omits Duveiller's kappa correction and therefore equals CCC; perfect negative association returns -1 while corrected lambda is zero. | Negative-covariance datasets leave canonical lambda's [0,1] range and have a different interpretation. | Document the CCC-like runtime formula and consider the corrected coefficient only as a separately approved compatibility change. |
| [ `NMAEp` ](metric-audit.md#metric-nmaep) | No located primary source establishes the exact name NMAEp with normalization by absolute observation mean. | The metric can be mistaken for a standard Lp norm or for other normalized p-error denominators. | Document the exact power-mean numerator and mean-observation denominator without implying a universal convention. |
| [ `Gini` ](metric-audit.md#metric-gini) | Runtime returns an unnormalized discrete ranking Gini rather than a [0,1] normalized Gini coefficient. | Perfect ranking depends on outcome prevalence (0.3 in the characterization), and reversed ranking is negative. | Document unnormalized signed scale, outcome-domain restrictions, and tie behavior; do not claim range [0,1]. |

## Validation gaps

| Metric record | Evidence | Impact | Reviewed future action |
| --- | --- | --- | --- |
| [ `NMSE` ](metric-audit.md#metric-nmse) | The implementation accepts opposite-sign means and returns a negative normalized squared error, while zero means silently return NaN. | Without a positive-mean domain, lower-is-better and nonnegative-error interpretations break down. | Document the unrestricted behavior now; consider explicit domain validation only in a separately reviewed behavior change. |
| [ `A10` ](metric-audit.md#metric-a10) | Undefined zero-reference ratios silently count as failures. | Exact zero predictions are penalized. | Define and document a zero policy. |
| [ `MNB` ](metric-audit.md#metric-mnb) | Zero observations are silently omitted. | Effective sample size changes. | Document omission. |
| [ `MNAE` ](metric-audit.md#metric-mnae) | The method accepts negative observations, allowing an absolute-error score below zero. | The implemented range and interpretation differ outside the positive concentration domain. | Document or validate the positive-observation domain. |
| [ `FB` ](metric-audit.md#metric-fb) | Signed inputs are accepted although the bounded interpretation assumes nonnegative concentrations. | Values can exceed [-2,2]. | Document or validate the nonnegative domain. |
| [ `FAE` ](metric-audit.md#metric-fae) | Negative inputs are accepted although nonnegativity is needed for the advertised bounded absolute-error interpretation. | FAE can be negative or exceed 2. | Document or validate input domain. |
| [ `GMB` ](metric-audit.md#metric-gmb) | Nonpositive pairs are omitted after a warning instead of rejecting the input. | The statistic may describe only an undocumented subset. | Document effective-sample behavior or require strictly positive inputs. |
| [ `FAC2` ](metric-audit.md#metric-fac2) | Zero-observation and negative pairs remain in N rather than enforcing a positive ratio domain. | Undefined ratios fail and negative pairs may pass. | Document or validate the positive domain. |
| [ `RMSD` ](metric-audit.md#metric-rmsd) | The denominator is signed rather than absolute. | Negative-mean observations produce a negative error magnitude. | Document or restrict the reference domain. |
| [ `MAD` ](metric-audit.md#metric-mad) | The normalizer is the signed observation mean. | Negative means make an absolute error negative and zero means are undefined. | Document or validate the intended positive-mean domain. |
| [ `SD` ](metric-audit.md#metric-sd) | The denominator is the signed observation mean. | Negative means reverse sign and zero means return NaN. | Document or validate the positive-mean domain. |
| [ `RSE` ](metric-audit.md#metric-rse) | p accepts values producing zero or negative degrees of freedom, nonintegers, and negative counts. | Calls can raise ZeroDivisionError or return NaN and can use nonsensical degrees of freedom. | Validate p as a nonnegative integer satisfying p < N-1 in a separately approved runtime change. |
| [ `KGE2012` ](metric-audit.md#metric-kge2012) | Zero means and constant agreement can raise raw ZeroDivisionError. | Degenerate inputs do not return a stable tuple shape. | Consider explicit denominator handling in a separately approved runtime change. |
| [ `KSI` ](metric-audit.md#metric-ksi) | normed is truth-tested rather than validated as bool. | Unexpected truthy values silently request normalized output. | Validate the parameter in a separately approved runtime change. |
| [ `SUSE` ](metric-audit.md#metric-suse) | n_bins is validated but no sample-size suitability rule is enforced. | Many empty bins can make small-sample entropy unstable while still returning a value. | Document bin-count sensitivity without changing runtime behavior. |
| [ `OVER` ](metric-audit.md#metric-over) | normed is truth-tested rather than validated as bool. | Unexpected truthy values silently select normalized output. | Validate in a separately approved runtime change. |
| [ `FoM` ](metric-audit.md#metric-fom) | Negative values are accepted although the min/max overlap is only bounded and area-like for nonnegative fields. | FoM may be outside 0-100 or misleading for signed data. | Document the nonnegative domain and consider separate validation approval. |
| [ `KLD` ](metric-audit.md#metric-kld) | Runtime silently converts signed arbitrary vectors to categorical distributions by absolute value and normalization; zero-sum NaNs collapse to zero. | Distinct invalid/undefined inputs can appear identical, and ordering semantics are easy to misread. | Document D(observation\|\|prediction), positional-category semantics, absolute conversion, and zero-sum behavior. |
| [ `BM` ](metric-audit.md#metric-bm) | Any c is accepted even though nonpositive values destroy the agreement-scale interpretation. | c=0 returns NaN and negative c can produce scores above one. | Document c=2 as the scientifically supported setting and consider positive-value validation separately. |
| [ `RNP` ](metric-audit.md#metric-rnp) | Zero-mean normalization yields NaNs that nansum can convert into alpha=1 while beta and the score remain NaN. | The component tuple mixes a misleading perfect alpha with undefined components. | Document positive nonzero-mean domain and define validation separately. |

## Duplicate and overlap groups

| Metric record | Evidence | Impact | Reviewed future action |
| --- | --- | --- | --- |
| [ `EC` ](metric-audit.md#metric-ec) | EC and R2 have identical formulas. | Two identities return the same number. | Document the overlap. |
| [ `R2` ](metric-audit.md#metric-r2) | R2 and EC are identical. | Two identities duplicate results. | Document the overlap. |
| [ `MNAE` ](metric-audit.md#metric-mnae) | MNAE and MAGE execute the same expression. | Two registry names return identical values for every accepted input. | Document the alias-like overlap. |
| [ `MAGE` ](metric-audit.md#metric-mage) | Implementation is exactly identical to MNAE. | The two names cannot distinguish scientific conventions at runtime. | Document the overlap. |
| [ `VAF` ](metric-audit.md#metric-vaf) | For every accepted input, VAF is algebraically exactly 100 * SBF. | Two names expose the same slope information on different scales. | Document the overlap and review registry intent. |
| [ `NMBF` ](metric-audit.md#metric-nmbf) | For positive means NMBF and MBF execute the same mean ratio; they differ only because MBF validates positive means. | Two registry identities return identical ordinary-domain values but advertise different domain behavior. | Cross-reference the overlap and domain distinction. |
| [ `RNMBF` ](metric-audit.md#metric-rnmbf) | RNMBF is exactly abs(NMBF-1), while runtime NMBF is the unrestricted raw mean ratio. | It overlaps RMBF on positive means but inherits a different domain policy. | Cross-reference RNMBF, RMBF, and NMBF and document their domain distinction. |
| [ `SS` ](metric-audit.md#metric-ss) | Runtime SS is algebraically identical to registered NSE on the same filtered pairs. | Two registry identities expose the same calculation without explaining the overlap. | Cross-reference SS and NSE and distinguish generic skill-score terminology from this implementation. |

## Test gaps

| Metric record | Evidence | Impact | Reviewed future action |
| --- | --- | --- | --- |
| [ `RNP` ](metric-audit.md#metric-rnp) | No pre-audit test directly exercised RNP or its four-component tuple. | Component order, normalized FDC arithmetic, and degeneracies could regress unnoticed. | Retain the characterization tests and document tuple component order. |
| [ `MEAN` ](metric-audit.md#metric-mean) | No pre-audit test pinned the observation-first two-tuple order. | Callers could silently transpose the two summaries. | Document and retain the tuple-order characterization. |
| [ `MEDIAN` ](metric-audit.md#metric-median) | No pre-audit test pinned the observation-first two-tuple order. | Callers could silently transpose the two summaries. | Document and retain the tuple-order characterization. |

## Documentation gaps

| Metric record | Evidence | Impact | Reviewed future action |
| --- | --- | --- | --- |
| [ `MB` ](metric-audit.md#metric-mb) | The method docstring says only 'Calculate mean bias' and does not state the subtraction direction. | Users can interpret the sign backwards because both conventions exist. | Document the prediction-minus-observation convention without changing runtime behavior. |
| [ `SpearmanR` ](metric-audit.md#metric-spearmanr) | The method docstring does not state that SciPy tie semantics are inherited or that constant input warns and returns NaN. | Users may expect a defined perfect score for identical constants or version-independent tie behavior. | Document the delegated SciPy semantics without changing runtime behavior. |
| [ `RE` ](metric-audit.md#metric-re) | The scalar reduction and zero omission are not apparent from the name. | Shape and weighting can surprise callers. | Document reduction and omission. |
| [ `A10` ](metric-audit.md#metric-a10) | No primary or authoritative source was established that uniquely owns the A10 name or definition. | The abbreviation cannot support a universal canonical interpretation. | Document the exact implemented threshold rule and application domain; add a domain source only when provenance is established. |
| [ `CI` ](metric-audit.md#metric-ci) | Categorical bands lack source context. | Contextual bands may be treated as universal. | Cite and qualify the bands. |
| [ `MNAE` ](metric-audit.md#metric-mnae) | No primary or authoritative source was identified that uniquely defines the exact MNAE name. | The registry name alone does not establish the denominator or units. | Document the runtime formula and cite a domain-specific definition if one is intended. |
| [ `SBF` ](metric-audit.md#metric-sbf) | A slope of one does not establish agreement when the unreported fitted intercept is nonzero. | Users may overinterpret the ideal value. | Document that slope assesses proportional response, not agreement alone. |
| [ `U95` ](metric-audit.md#metric-u95) | The name does not state the assumptions required for 95% coverage. | Users may interpret the index as a calibrated interval. | Describe U95 as a model-evaluation composite, not guaranteed coverage. |
| [ `NNSE` ](metric-audit.md#metric-nnse) | The docstring calls the transformation more objective without source support. | A mathematical remapping may be mistaken for an improved evidential criterion. | Remove or substantiate the objectivity claim in a separately approved documentation change. |
| [ `RAE` ](metric-audit.md#metric-rae) | The docstring says RMSE over root sum of squared observations, while the numerator is root sum of squares. | The stated formula differs by a factor involving sample size. | Correct the formula description separately. |
| [ `LCE` ](metric-audit.md#metric-lce) | The abbreviation LCE is not the paper's standard notation and overlaps conceptually with published least-squares combined efficiency. | Users may confuse two unrelated metrics. | Cross-reference E1 and disambiguate LCE versus LCEf. |
| [ `PHI` ](metric-audit.md#metric-phi) | The registered name says Percentage although runtime returns the conventional normalized fraction without multiplying by 100. | Users may interpret 0.5 as 0.5 percent instead of 50 percent. | Document fraction units and bin sensitivity. |
| [ `STD` ](metric-audit.md#metric-std) | The method name does not state that runtime uses population normalization ddof=0 and observations only. | Users expecting sample standard deviation obtain a smaller value. | Document observations-only scope and ddof=0. |
| [ `nESkew` ](metric-audit.md#metric-neskew) | The Correndo et al. attribution could not be verified; runtime uses local normalization plus bias-corrected sample skewness and requires three points. | Users lack a traceable scientific identity and small samples differ from population-moment calculations. | Correct the attribution if a source is found; meanwhile document SciPy bias=False and minimum sample size. |
| [ `nEKurt` ](metric-audit.md#metric-nekurt) | The Correndo et al. attribution could not be verified; runtime returns bias-corrected Fisher excess kurtosis and requires four points. | Users lack a traceable scientific identity, and values differ by three from Pearson convention and from biased estimates. | Correct the attribution if a source is found; meanwhile document fisher=True, bias=False, and minimum sample size. |
| [ `RMBF` ](metric-audit.md#metric-rmbf) | No independent primary scientific source was established for the exact unsigned abs(MBF-1) quantity under the RMBF name. | Users cannot assume equivalence to other relative or symmetric bias-factor definitions. | Present RMBF explicitly as the package's unsigned transformation of MBF. |
| [ `MSDdec` ](metric-audit.md#metric-msddec) | The tuple exposes four unlabeled positional scalars and regression orientation is only discoverable from helper code. | Callers can transpose component meaning or compare against the reciprocal regression convention. | Document tuple order and observation-on-prediction regression orientation prominently. |
| [ `MPE` ](metric-audit.md#metric-mpe) | Runtime omits zero-observation cases from both numerator and effective count without documenting that sample-dependent policy. | Large errors at zeros disappear and results may look better as zero frequency rises. | Document omission and effective sample behavior. |
| [ `MAPE` ](metric-audit.md#metric-mape) | Runtime silently omits zero actuals rather than treating canonical MAPE as undefined for the dataset. | Errors at zeros are invisible and the averaging population changes. | Document zero omission and MAPE's asymmetry; consider explicit policy only in a separately approved runtime change. |
| [ `sMAPE` ](metric-audit.md#metric-smape) | Runtime silently omits pairs where prediction and observation are both zero. | The effective sample count changes and perfect zero forecasts do not contribute zero error; an all-zero perfect series returns NaN. | Document the zero-pair policy and exact 0-200 scaling. |
| [ `CRPS` ](metric-audit.md#metric-crps) | The registered probabilistic name accepts only one scalar prediction per observation and computes MAE. | Users cannot evaluate ensemble or distribution forecasts and may infer probabilistic support that is absent. | Document that this is deterministic point-mass CRPS only. |
| [ `TAcc` ](metric-audit.md#metric-tacc) | No source for the exact formula was located. | Its scientific identity and interpretation are unclear. | Document the formula as a fitted-slope similarity and source it if possible. |
| [ `dCor` ](metric-audit.md#metric-dcor) | Runtime uses the biased empirical estimator and returns zero for degenerate constant inputs without identifying the estimator convention. | Finite-sample values differ from unbiased variants and zero does not establish independence for a constant variable. | Document biased double-centering and constant-series semantics. |
| [ `SMA` ](metric-audit.md#metric-sma) | Seven positional components, regression orientation, population SD convention, and NaN-correlation replacement are not exposed by the registry description. | Callers can transpose slope orientation or tuple meanings and miss constant-series substitution. | Document tuple order and degeneracy policy prominently. |
| [ `TSS` ](metric-audit.md#metric-tss) | Runtime fixes the Taylor-score exponent and reference correlation without exposing those conventions. | Values cannot be compared safely with generalized Taylor skill-score variants unless conventions are known. | Document exponent four, reference correlation one, and population-SD behavior. |
| [ `CRMSE` ](metric-audit.md#metric-crmse) | The registered description does not explain that independent mean biases are removed. | A zero score can occur for predictions with an arbitrarily large constant offset. | Document CRMSE as centered-pattern error and pair it with a bias summary. |
| [ `PCD` ](metric-audit.md#metric-pcd) | Runtime requires a strictly positive product, so matching flat transitions do not count as correct, and preprocessing can compress time. | Perfect series with plateaus score below one and missing-value removal changes which transitions are evaluated. | Document strict-direction and time-compression semantics. |

## Unresolved source questions

These are provenance questions preserved by the batch research, not inferred resolutions:

- `A10`: no primary or authoritative source uniquely establishing the A10 name was located ([batch 2](research/metric-audit-batch-2.md)).
- `MNAE`: no primary or authoritative source uniquely defining the exact name was located ([batch 3](research/metric-audit-batch-3.md)).
- `nESkew` and `nEKurt`: the stated Correndo et al. attribution could not be verified ([batch 6](research/metric-audit-batch-6.md)).
- `RMBF`: no independent primary source for the exact unsigned transformation under this name was established ([batch 6](research/metric-audit-batch-6.md)).
- `TAcc`: no source for the exact fitted-slope formula was located ([batch 8](research/metric-audit-batch-8.md)).
- `NMAEp`: no located primary source establishes the exact name and denominator convention ([batch 9](research/metric-audit-batch-9.md)).

## Non-binding Phase 2 proposal

Every proposal below requires separate approval. Ordering reflects scientific risk first, then compatibility impact and implementation dependency; it is not authorization to change behavior.

| Tier | Proposal | Scientific risk | Compatibility impact | Dependency | Approval |
| --- | --- | --- | --- | --- | --- |
| 1 | Resolve the intended identity/formula for `FB`, `MAGE`, `RAE`, `VAF`, `DE`, `WIAr`, `OVER`, `NMBF`, `AD`, and `NAE`; specify edge semantics for `TS`, `TAcc`, `iqRMSE`, and `MSLE`; implement `MASE.m` only after its contract is fixed. | High: reviewed records identify possible formula, sign, domain, or ideal-value defects. | High: values, ranges, exceptions, names, or parameter behavior may change. | Scientific decision and migration policy before code. | Separate approval required |
| 2 | Define shared domain and denominator policies, then address metric-specific validation gaps such as positive-domain ratios, zero means, constant series, sample size, boolean flags, and degrees of freedom. | Medium: invalid inputs currently yield misleading values, NaN, infinity, or low-level errors. | Medium to high: new validation or explicit edge results can break callers. | Tier 1 decisions plus a package-wide compatibility policy. | Separate approval required |
| 3 | Document standard variants and units, tuple/component order, effective-sample rules, and the overlap groups `EC`/`R2`, `MNAE`/`MAGE`, `SBF`/`VAF`, `MBF`/`NMBF`/`RMBF`/`RNMBF`, and `NSE`/`SS`; retain the new characterization coverage. | Low when documentation-only; higher if aliases are deprecated. | Low for documentation and tests; potentially high for registry cleanup. | Can start after Tier 1 naming decisions; registry changes remain separate. | Separate approval required |
